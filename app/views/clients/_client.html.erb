<div id="<%= dom_id client %>">
  <tr>
    <th scope="row"><%= client.id %></th>
    <td><%= client.name %></td>
    <td><%= client.age %></td>
    <td><%= client.movies.last %></td>
    <td><%= link_to "Show this client", client %></td>
    <td><%= link_to "Edit this client", edit_client_path(client) %></td>
    <td><%= link_to "Destroy this client", client_path(client), data: {
      turbo_method: :delete,
      turbo_confirm: "Estás seguro?"
    } %></td>
  </tr>
</div>
